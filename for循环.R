fib <- numeric(16)  # 创建一个长度为16的空数列

fib[1] <- 0  # 初始化第一个数为0
fib[2] <- 1  # 初始化第二个数为1

for (i in 3:16) {
  fib[i] <- fib[i-1] + fib[i-2]  # 计算第i个数为前两个数的和
}

print(fib)  # 输出斐波那契数列前16个数,在R语言中，用于显示（显示）向量或对象的函数是print()或直接在代码中使用对象名称。show()函数在R中用于显示对象的内部结构，通常在调试过程中使用
